/**
 * Deterministic seeded random numbers (`@furnace/core/rng`) — replay-safe
 * randomness for procedural generation and any reproducible simulation.
 */
export { create } from "./rng.ts";
export type { Rng } from "./types.ts";
