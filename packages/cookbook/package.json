{
  "name": "@furnace/cookbook",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "bun scripts/generate-help-manifest.ts",
    "dev": "bun --hot serve.ts",
    "prebuild:web": "bun scripts/generate-help-manifest.ts",
    "build:web": "bun scripts/build.ts",
    "prebuild:web:dev": "bun scripts/generate-help-manifest.ts",
    "build:web:dev": "bun scripts/build.ts --dev",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "@furnace/core": "workspace:*"
  },
  "devDependencies": {
    "bun-plugin-svelte": "^0.0.6",
    "svelte": "^5.55.7"
  }
}
