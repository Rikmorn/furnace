{
  "name": "@furnace/cookbook",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot serve.ts",
    "build:web": "bun scripts/build.ts",
    "build:web:dev": "bun scripts/build.ts --dev",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "@furnace/core": "workspace:*"
  },
  "devDependencies": {
    "bun-plugin-svelte": "^0.0.6",
    "svelte": "^5.55.7"
  }
}
