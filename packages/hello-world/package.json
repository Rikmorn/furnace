{
  "name": "@furnace/hello-world",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot serve.ts",
    "dev:native": "furnace dev --platform=macos",
    "build:web": "bun build src/index.html --outdir ../../dist/web --minify --sourcemap=external",
    "build:web:dev": "bun build src/index.html --outdir ../../dist/web/dev --sourcemap=inline",
    "build:macos": "furnace build --platform=macos",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit",
    "edit": "bun run --cwd ../editor build:frontend && bun ../editor/src/daemon/main.ts"
  },
  "dependencies": {
    "@furnace/core": "workspace:*"
  },
  "devDependencies": {
    "@furnace/editor": "workspace:*",
    "@furnace/tools": "workspace:*",
    "bun-plugin-svelte": "^0.0.6",
    "svelte": "^5.55.7"
  }
}
