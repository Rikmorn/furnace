{
  "name": "furnace",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "hello-world:dev": "bun run --cwd packages/hello-world dev",
    "hello-world:dev:native": "bun run --cwd packages/hello-world dev:native",
    "hello-world:build:web": "bun run --cwd packages/hello-world build:web",
    "hello-world:build:web:dev": "bun run --cwd packages/hello-world build:web:dev",
    "cookbook:dev": "bun run --cwd packages/cookbook dev",
    "cookbook:build:web": "bun run --cwd packages/cookbook build:web",
    "core:build": "bun run --cwd packages/core build",
    "tools:build": "cargo build --manifest-path packages/tools/crates/Cargo.toml --release",
    "dungeon:dev": "bun run --cwd packages/dungeon dev",
    "dungeon:editor": "bun run --cwd packages/dungeon edit",
    "build": "bun run core:build && bun run tools:build && bun run hello-world:build:web",
    "typecheck": "bun run --cwd packages/core typecheck && bun run --cwd packages/hello-world typecheck && bun run --cwd packages/cookbook typecheck && bun run --cwd packages/editor typecheck && bun run --cwd packages/dungeon typecheck",
    "test": "bun test",
    "check": "biome check && bun run --cwd packages/core check:tsdoc",
    "clean": "rm -rf dist packages/*/dist packages/*/crates/target packages/*/.furnace/shell/target packages/*/plugins/*/pkg packages/*/plugins/*/target"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.15",
    "@types/bun": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
