---
status: queued
after: vocabulary-expansion
summary: the huge-world slice (scale, formerly F5) — held last by ruling
---

# Huge worlds — scale

The huge-world slice: making the cockpit's loop hold at a world size the current one does
not reach.

Held to LAST by ruling — every injected item ahead of it (see the `after:` chain) was
judged cheaper to take before scale, not after, because scale changes the cost of changing
anything else.

**The handoff register is `docs/reference/engine-architecture.md` §16**, "Huge-world
realization assumptions". It was written across earlier slices as each assumption was
discovered rather than at F5 planning time, so it is the input to read first.

**Not yet planned.** This file is the placeholder that keeps the epic honest about what is
left; planning it is its own session.
