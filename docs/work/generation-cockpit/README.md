---
status: in-flight
summary: the cockpit epic — generate/dig/paint → bake → walk, and everything the loop needs to be real
---

# Generation cockpit (Epic 3)

The loop is real: generate, dig, paint, bake, walk. What remains is scale and the work
that has been injected ahead of it.

**Exit condition:** the owner is fully happy with everything the epic uncovered — not a
task list running out. Ruled at the foundations T5 review, and it is deliberately a
judgement rather than a checklist, because every cycle of this epic has surfaced more than
it closed.

**Jurisdiction:** *if you can dig it, it's field.* That line settles most "which subsystem
owns this?" questions on sight.

**Shipped:** 3.0 → 3.3, then the ONE FIELD slices F0–F4, then F4.5 (the overlay cockpit —
shell, hands, finish). Each is sealed; the seals under `docs/learnings/seals/` are the
record, and `docs/reference/editor-architecture.md` is the as-built.

**Remaining:** F5 (scale) — the huge-world slice, held last by ruling. Its handoff register
is `docs/reference/engine-architecture.md` §16, which is where the realization assumptions
that F5 has to satisfy were written down as they were discovered.
