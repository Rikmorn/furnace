# Seal log — chronological slice/epic seals (append-only)

Extracted VERBATIM from `AGENTS.md`'s per-package bullets on 2026-07-06 (post-Slice-3.1
hygiene): the bullets had grown to ~50 KB of history loaded into every agent session.
AGENTS.md now carries CURRENT-STATE summaries + pointers; the full chronological record
lives here and future slice seals APPEND here (a dated `##` section per seal), with
AGENTS.md getting only its current-state delta. As-built architecture is distilled in
`docs/reference/` (`dungeon-architecture.md`, `editor-architecture.md`, `core-modules.md`).

## The record as sealed through Epic 3 · Slice 3.1 (2026-07-06)

### packages/core (bullet as of the extraction)

- `packages/core/` (`@furnace/core`, private) — the engine library. Exposes its public surface as per-feature sub-path modules — `@furnace/core/{gpu, frame, geometry, mesh, material, camera, transform, post, events, stats, input, log, resources, physics, rigid-mesh, rng, scene}` — documented signature-by-signature in `docs/reference/core-modules.md` (concept taxonomy + naming rules in `docs/reference/api-posture.md`). Engine-wide conventions are committed at `docs/reference/engine-conventions.md`. **Browser-only**: no framework deps, no Bun coupling in core's source. The `tests/no-bun-leakage.test.ts` static scan is one guardrail; the full contract lives in "What we ship to consumers" below. Future wasm hot-path crates (transforms, audio) will live here. Contains no native binaries. (Slice 2.1.1 added `physics.castRay`/`castShape` query primitives — generic ray/shape-sweep probes consumers can build movement solvers on; both also return the hit body via `RayHit.body`. Slice 2.2.1 added the `voxels` `ShapeDescriptor` variant — a sparse, ghost-free static/kinematic voxel collider for field-derived level geometry, wrapping Rapier's voxel collider; heightfield was deliberately NOT added — it is broken in the vendored `rapier3d-compat@0.19.3` wasm.) **Slice 2.2.3a ("GPU instancing") added first-class instancing:** `mesh.createInstanced(ctx, { geometry, material, count })` → an `InstancedMesh` resource (own pool, dispose-cascade slot, `stats.resources.instancedMeshes` count) with per-instance setters `setInstanceTransform` (TRS) / `setInstanceTint` / `setInstanceMatrices` (bulk, column-major) / `setInstanceCount` (drawn prefix) / `destroyInstanced`; per-instance data rides a `stepMode:"instance"` vertex buffer (baked `mat4` at vertex-buffer slot 1, tint `vec4` at slot 2). Instanced shader variants `shader.litInstanced` / `shader.unlitInstanced` (+ `shader._instancedOf`) source the model matrix from those attributes, **bypass `@group(2)`**, and multiply a per-instance tint into the colour/albedo — **uniform per-instance scale only** (documented precondition: the lit variant derives the normal from `mat3(model)`). Instanced meshes are passed via a **separate** `instanced?: InstancedMesh[]` param on **both** `frame.render` (`RenderOptions`) and `renderToTexture` (`RenderToTextureOptions`) — deliberately **NOT** a `(Mesh|InstancedMesh)[]` union (overturned the spec's §3.5 union-list): `Mesh` and `InstancedMesh` handles collide numerically (both pools allocate from slot 1 → identical `encodeHandle(ctxId,1,1)`), so a runtime union can't soundly discriminate kind — the engine keeps drawable kinds in typed-distinct lists (its "kind known from typed slot, never inferred from a bare handle" invariant). Instanced groups draw after `meshes` in the same pass; emissive groups glow through the existing post chain. The per-instance-tint pixel-readback test also surfaced + fixed a pre-existing `renderToTexture` bug: RTT never called `_flushDirtyBindings`, so binding-set material uniforms rendered as their zero-initialised GPU value (black). Instanced meshes do **not** cast shadows yet (deferred — `docs/backlog/engine-architecture/shadow-follow-ons.md`, *Instanced meshes don't cast shadows* section); a bulk `setInstanceTints` to mirror `setInstanceMatrices` is deferred (`docs/backlog/engine-architecture/bulk-set-instance-tints.md`). Headless: `tests/gpu/instanced-mesh.gpu.test.ts` covers create/setters/draw + the per-instance-tint readback + the RTT flush fix. The `docs/backlog/engine-architecture/first-class-gpu-instancing.md` entry is resolved + deleted.)

### packages/dungeon (bullet as of the extraction)

- `packages/dungeon/` (`@furnace/dungeon`, private) — the first-person dungeon-crawler demo (the retargeted go-forward app — see `~/.claude/.../memory/project_dungeon_crawler_vision.md`). Browser-first consumer; imports core via the workspace symlink; owns its own `index.html` + `serve.ts`; run `bun run dungeon:dev`. **Epic 1 ("Presence & Mood") landed:** a hand-authored corridor→cave level on lit box primitives, a pointer-lock FP controller (raw DOM `movementX/Y` — `@furnace/core/input` has no relative-pointer mechanism; deferred to backlog), a carried flickering torch (point light follows the player), distant unlit-bloom glows over an HDR `bloom→tonemap` post chain, exponential distance **fog** (the one `@furnace/core` mechanism this epic added — `@furnace/core/frame` `Fog`), and optional additive drifting motes. **Epic 2 · Slice 2.0 ("Player Physics") then landed:** Epic 1's hand-rolled game-side slide collision (`slideMove`) was retired for a **real Rapier capsule + kinematic character controller**. Core gained the physics mechanism — capsule shape, `kinematicPosition` body, `setBodyNextKinematicTranslation`, and a world-owned character controller (`createCharacterController`/`computeMovement`/`destroyCharacterController` tracked on `WorldSlot.controllers`; `applyImpulsesToDynamicBodies`/`characterMass` enable shoving dynamic bodies) — all thin wraps over the vendored `@dimforge/rapier3d-compat`. The dungeon owns policy: a physics world with static cuboid colliders from `LEVEL_BOXES`, a kinematic player capsule driven by the controller (gravity/slide/step-up/grounded; `FpController` emits a desired per-tick move + places the camera from the body, which now owns position, with vertical velocity via `gravityStep`), shovable dynamic props + immovable feature blocks, and an enlarged level (side branch + second chamber). **Epic 2 · Slice 2.1 ("Field→Baked Region") then landed:** the clean break from box-AABB geometry. Core gained: `@furnace/core/rng` (seeded sfc32 PRNG with `float/int/bool/pick/derive`); physics `trimesh` collider variant (`ShapeDescriptor`); `geometry.create` `{ retainForCollision }` opt + `geometry.getCollisionData`; scene `"mesh"` geometry resource (fetches + decodes a `.fmesh`); `rigidBody.shape.trimesh: true` (boolean flag → pulls collision arrays from the sibling geometry); `settings.region` metadata block (`{ provenance?, theme?, origin? }`); `loadScene` `world`-injection + `fragment` mode (`LoadSceneOptions`); and the `encodeMeshBlob`/`decodeMeshBlob` `.fmesh` codec. The dungeon adds a density-field pipeline (`f(seed, pos) → density`) run through a naive Surface Nets mesher that produces three procedurally generated rooms on the authored hand-carved spine: a **cavern grotto** (a descent-into bowl), a **vertical shaft** (a drop), and a **walk-in bumpy chamber floor**. The chamber floor region is baked to a `.fmesh` sidecar file + a region scene document by a throwaway one-shot baker script, then loaded back via `loadScene({ world, fragment: true })` into the live game world — exercising the full persistence round-trip. The real Rapier trimesh collider replaces the box-AABB approach for the generated geometry. Chamber floor visual polish (seams, visibility tuning) is deferred to `docs/backlog/dungeon/chamber-floor-seams-and-visibility.md`. Headless: the core rng, physics trimesh surface + scene round-trip are `.gpu.test.ts`-covered (or unit-tested for pure math); the dungeon's pure math (`gravityStep`/`moveDelta`/`forwardVector`) and the `.fmesh` codec are unit-tested; generated room feel is gated visually (Safari/Chrome), not by the suite. **Epic 2 · Slice 2.1.1 ("Traversal Foundation") then landed (2026-06-18):** the generated trimesh floor wasn't walkable — a Rapier capsule stalls on the mesh's internal-edge ghost collisions — so the dungeon now drives the player with a custom `CharacterMover` (collide-and-slide built on the new core `physics.castRay`/`castShape` primitives: a horizontal shapecast slide pass + a separate downward-ray gravity/ground pass that snaps to the *true* ground normal + explicit step-up) instead of Rapier's built-in KCC, which **stays available in core, unused, as a future option**. The Slice 2.1 box-floor chamber workaround was removed (the generated floor is now the real walking surface), and convex ramp/step gate fixtures were added. The custom controller also **restores shoving dynamic props** — via core `RayHit.body` + a standalone `shoveDynamicBodies` helper that velocity-pushes a hit dynamic body — and **smooths the camera eye-height** to soften the vertical jumps from steps / bumpy terrain. The custom controller's pure math (`clipVelocity`/`isWalkable`) is unit-tested and its slide/ground/step passes are `.gpu.test.ts`-covered; traversal feel stays a visual gate. **Epic 2 · Slice 2.2.1 ("World collision via field-derived voxels") then landed (2026-06-22):** the three generated regions (cavern, shaft, chamber) now collide against ghost-free **field-derived Rapier voxel colliders** instead of the internal-edge-prone render trimesh — killing the curved-wall-stuck / floor-stall / hall↔chamber fall-through bug class at the root (the same internal-edge ghost collisions the 2.1.1 controller works *around*, now removed from the collision representation itself). Core gained one thin static shape — the `voxels` `ShapeDescriptor` variant (`{ coords: Int32Array; size }`, signed int grid coords 3-per-voxel, wrapping `RAPIER.ColliderDesc.voxels`; **heightfield was excluded — broken in the vendored 0.19.3 wasm**). The dungeon adds `packages/dungeon/src/proxy.ts` (`voxelsFromField` sign-samples each grid cell centre → solid-cell coords, shell-only drops fully-enclosed interior cells; `voxelProxyPosition` seats the proxy body — Rapier voxels are corner-anchored, so `HALF_VOXEL = 0` aligns voxel cell (i,j,k) with its field/render cell) plus a generator `generateProxy` (field-only, no Surface-Nets) so each region carries a `proxy` + `proxyPosition` alongside its `mesh`. The shaft + chamber collide against `region.proxy`; the **cavern is now render-only** (its scene dropped `rigidBody`) with a voxel proxy regenerated at runtime from the same seed/kind/origin as the baked mesh (deterministic → matches). Authored `LEVEL_BOXES` stay `cuboid`; dynamic props stay Rapier dynamic bodies; the `CharacterMover` is **unchanged** (it casts against colliders uniformly, never distinguishing voxel from cuboid). The chamber grid's +Z footprint was widened (dims.z 12→14) to **overlap** the authored corridor floor, closing the hall↔chamber seam by coverage. The accepted tradeoff is voxel blockiness, absorbed by the 2.1.1 camera eye-height smoothing plus **anisotropic-Y voxels** — `buildProxy` voxelizes at `voxelSize=[cellSize, 0.25, cellSize]` (`PROXY_VOXEL_Y=0.25`), so a floor-height quantization step is 0.25 m (< the controller's 0.4 m `STEP_HEIGHT`) instead of a stalling 0.5 m cubic cell. Three gate rounds hardened it: `voxelsFromField` treats **off-grid neighbours as solid rock** in the shell-only enclosure test (a grid-boundary rock cell is kept only if it borders *in-grid* air → the collision shell follows the rendered isosurface, not the grid box → kills an invisible grid-boundary collision wall); the cavern field is a `boxCavern` sized to its authored square pit (no rim poking up through the chamber floor; `.fmesh` re-baked to match); and `char-move.ts applyGravity` takes its rest height from a downward capsule **shapecast** (rim-riding — rests on the highest support in the footprint instead of a single ray sinking the capsule into a sub-capsule voxel pocket). Headless: the core `voxels` shape (ray/shapecast + ball-rest), the CharacterMover-traverses-voxels (chamber walk + shaft drop), the seam-coverage probe, and a `traversal.gpu.test.ts` fuzz harness (every lane walks the full chamber, no wedge, no fall-through) are `.gpu.test.ts`-covered; `voxelsFromField`/`voxelProxyPosition` are unit-tested; traversal feel stays a visual gate (passed Safari/Chrome). The core `trimesh` shape + the scene-loader trimesh-collision path (`retainForCollision`/`getCollisionData`/scene `rigidBody.shape.trimesh`) are **kept** (tested public API; scene-baked collision belongs to 2.3 recipe-as-truth). The forward direction (voxels are a confirmed *bridge*; endgame = collide the render mesh on Jolt's `CharacterVirtual`) lives in `docs/backlog/engine-architecture/jolt-backend-swap.md` + `docs/learnings/jolt-mesh-collision-spike.md`. **Epic 2 · Slice 2.2.2 ("Theme-Generator Architecture") then landed (2026-06-24):** the three hardcoded single-kind cave configs were replaced by a pluggable, pure, deterministic theme-generator architecture. `packages/dungeon/src/region.ts` defines the `RegionData` contract (meshes + colliders + materials + `Connection`s + provenance) and a `themes` registry of pure `(RegionParams) → RegionData` generators; `realize.ts` (`realizeRegion` + `MaterialCache`) turns that data into GPU meshes + static colliders into a world (one shared material per distinct descriptor); `compose.ts` (`buildArea`) assembles a branching cave + a per-branch vestibule + a room via cardinal-snap placement (yaw-mapped door→mouth join). Three themes: a branching SDF `cave` (`themes/cave.ts` — a hub + floor-routed capsule tunnels via new `field.ts` primitives `capsuleCavern`/`smoothUnion`/`yTaperedNoiseDisplace`, meshed by Surface-Nets and collided by a field-derived voxel proxy) and box-architecture `pillarHall`/`greatHall` (a shared `themes/box-room.ts` builder → cuboid walls/doorway/pillars/dais+steps with `cuboid` colliders). The flat-floored vestibule (`compose.ts`) bridges each cave mouth to a room door so the cave voxels only ever overlap a coplanar cuboid floor — the proven 2.2.1 seam geometry. The single-kind `generator.ts` (the retired shaft/chamber kinds + the `Pass`/`RegionModel` scaffolding) was deleted; its surviving baked-cavern path is preserved byte-for-byte as `bakedCavernProxy` in `themes/cave.ts` (the cavern still renders from its `.fmesh` and collides against this runtime-regenerated voxel proxy). No `@furnace/core` change. Headless: `region.ts`/`compose.ts` placement math is unit-tested, `bakedCavernProxy` determinism is unit-tested (`tests/cavern-proxy.test.ts`), and the composed walk (`area-traversal.gpu.test.ts` — cave→vestibule→room, plus the greatHall dais climb) and the re-pointed full-world fuzz harness (`traversal.gpu.test.ts` — cave-hub lanes, the level↔wing seam walk, and a no-fall-through floor-ray grid over the authored 2nd chamber) are `.gpu.test.ts`-covered; theme/composition feel stays a visual gate. **Epic 2 · Slice 2.2.3a ("Decorative scatter") then landed (2026-06-26):** a pure, deterministic decorative scatter system that dresses the generated regions with GPU-instanced props (consuming core's new instancing — see the `packages/core` bullet). `packages/dungeon/src/scatter.ts` area-weight-samples the generated Surface-Nets mesh (`meshSurface` + `_sampleSurface` — triangle-area CDF + barycentric √-debias) — with a flat-floor `rectSurface` adapter for box rooms — then `scatter()` applies slope/density/keep-out **masks** + blue-noise **dart-throwing** spacing + per-instance variation (scale/rotation/tint jitter) + surface-aligned **orientation** (`_orient`, incl. ceiling-hang anchoring so ceiling items dangle *into* the room); `instanceGroupsFromLayers` bakes the results into GPU-ready `InstanceGroup`s (column-major transforms, grouped by archetype variant, materials deduped). The region contract gains a `RegionData.instances: InstanceGroup[]` seam fed by theme-declared `ScatterLayerSpec` layers (each a `MaterialPosture` `lit` | `emissive`); `realize.ts` does posture-aware instanced realization (`lit` → `litInstanced`, `emissive` → `unlitInstanced` + bloom — **emissive scatter is purely visual glow, NOT real light**: real scattered lights stay deferred behind the 16-light forward cap → `docs/backlog/engine-architecture/many-lights-clustered-or-deferred-shading.md`); `main.ts` renders the groups via `frame.render({ instanced })`. Content: a **5-layer cave showcase** (rubble + crystal-spires `lit`; glow-fungus + wall-crystals + glow-worms `emissive` — spanning floor / wall / ceiling, the ceiling glow-worms hanging down) plus a lean box-room floor scatter. **Coordinate frames:** the cave bakes **world** transforms (local scatter offset by the region origin) while rooms bake **local** transforms that `compose.ts placeRoom` transforms into world. Archetypes are **uniform-scaled unit primitives** (cube / sphere / cylinder) — real procedural archetype meshes (the worm/spire silhouette) are deferred (`docs/backlog/dungeon/visual-polish-pass.md`, *real procedural archetype meshes for decorative scatter* section). Decoration is **non-colliding** — collision + walking are unchanged. No `@furnace/core` change beyond the consumed instancing. Headless: `tests/scatter.test.ts` unit-tests the sampling/mask/spacing/orient math (deterministic), `tests/scatter-realize.gpu.test.ts` covers the realize→GPU path; scatter feel stays a visual gate (passed Safari/Chrome). **Epic 2 · Slice 2.2.3b ("Colliding fixtures + interactive props") then landed:** scatter layers gained a `collision` posture (`"solid" | "dynamic"`, absent = decorative ghost) on `ScatterLayerSpec`/`InstanceGroup` (+ `InstanceGroup.placements` = the baked-frame pos/rot/scale). `realize.ts` interprets it: `solid` → one static primitive collider per instance (`colliderFor`: cube→cuboid / sphere→ball / cylinder→cylinder, uniform-scaled, no trimesh → ghost-free); `dynamic` → one shovable rigid body per instance + per-frame instanced-render sync (`update()`) + an addressable `DynamicProp` record (the seam for later pickup/throw/`query` verbs). Posture consumes no RNG (toggling it never reshuffles placements); dynamic props spawn lifted (`DROP_MARGIN`) and settle via friction/damping/auto-sleep. `compose.ts placeRoom` transforms room groups' `placements` into world frame. Content: cave `spires` → solid, box-room `roomCrates` → dynamic. The hand-placed `props.ts` was **retired** — loose crates are now a dynamic scatter layer (single source of truth). No `@furnace/core` change. Headless: `tests/scatter.test.ts` (posture/placements carry + determinism + dynamic lift), `tests/scatter-realize.gpu.test.ts` (solid supports a dropped ball; dynamic shove + `update()` tracks), `tests/compose.test.ts` (`placeRoom` world-frame placements incl. a rotation cross-check), `tests/cave.test.ts` (spires solid); collision feel stays a visual gate. **Epic 2 · Slice 2.2.4 ("Seams — the connection primitive") then landed (2026-06-29):** today's three special-cased connection mechanisms — cardinal-snap room↔cave join, the flat `vestibule` bridge, and the hand-tuned `AREA_ORIGIN` authored↔generated seam — were replaced by ONE general portal-join + walkable-connector primitive, proven on a hand-composed multi-level arrangement of the existing dungeon. A new `packages/dungeon/src/walkability.ts` single-sources the locomotion constants (`STEP_HEIGHT`/`STEP_MARGIN`/`SLOPE_LIMIT_RAD`/`SLOPE_LIMIT_COS`) that `char-move.ts`, `themes/box-room.ts`, and the new router all read — so connectors are walkable by construction (the `GROUND_SNAP >= STEP_HEIGHT` invariant, with `GROUND_SNAP` still owned by `char-move.ts`, is now unit-asserted; `box-room.ts`'s duplicate `STEP_HEIGHT` is retired, now re-exported from `walkability.ts`). `packages/dungeon/src/connect.ts` holds the pure join + connector-routing primitives: `join(a, b)` computes the continuous-yaw + height `Placement` seating portal B's opening onto portal A (B's outward facing → A's INWARD facing −A.facing — the general case of the retired cardinal-snap), and `placePiece(region, place)` applies it across a whole `RegionData` (mesh/collider positions rotated+translated, per-piece rotation composed with the yaw, connection facings rotated, instance transforms + placements transformed). `route(from, to, opts?)` returns a connector `RegionData` walkable by construction — `chooseKind` picks **corridor** (flat) / **ramp** (one pitched slab gated by the slope limit) / **stairs** (reusing `box-room.ts stepBoxes`, each riser < `STEP_HEIGHT − STEP_MARGIN`) from the height delta over the run, or `opts.kind` forces one (setup-loud throw when a forced ramp's pitch exceeds the slope limit); the connector floor overlaps both portals by ≥1 cell (`SEAM_OVERLAP`). The `region.ts` contract gains `RegionMesh.rotation?`/`RegionCollider.rotation?` (quaternion, absent = identity) + `Provenance.theme: RegionKind` (= `ThemeName | "connector"`); `realize.ts` applies mesh rotation via `mesh.setRotation` and collider rotation via `createBody({rotation})` (the body-rotation path scatter already used). The migration: `compose.ts placeRoom` is now `placePiece(room, join(target, door))` (cardinal `yawMap`/`rotateExtent`/`rotateShape` deleted); `buildArea` gaps each branch room `ROOM_GAP` beyond the cave mouth and bridges it with a `route` corridor (`vestibule` + `snapCardinal` deleted — the corridor IS the vestibule's flat case from the general primitive); a shared `attachWing(seed, chamberDoor, gap)` helper seats the generated wing onto the authored chamber's exported `CHAMBER_DOOR` Connection via `join` + a `route` corridor (deleting `main.ts`'s `AREA_ORIGIN` GATE-TUNE), called by BOTH `main.ts` and the `traversal.gpu.test.ts` harness so they can't drift. The proof: `attachUpperLevel` adds a hand-composed second storey on the existing dungeon — a `pillarHall` reached by a ~30° **off-axis** ramp (a non-cardinal join yaw, exercising the rotation pipeline end-to-end) and a `greatHall` reached by a **cardinal stair-run**. End state: **one join path in the codebase**. **No `@furnace/core` change** (`mesh.setRotation` + `BodyDescriptor.rotation` already existed). Headless: `tests/walkability.test.ts` (`GROUND_SNAP`/slope invariants), `tests/connect.test.ts` (`join`/`placePiece` general + cardinal-regression, `route` kind-selection + walkable-by-construction + forced-ramp throw + seam overlap), `tests/connect.gpu.test.ts` + `tests/realize-rotation.gpu.test.ts` + the `area-traversal.gpu.test.ts` ramp/stair climbs (the CharacterMover walks routed ramps + stairs + the off-axis upper room), and the re-anchored `traversal.gpu.test.ts` (cave-hub fuzz + level↔wing `attachWing` seam) are `.gpu.test.ts`-covered; placement/route math is unit-tested; the multi-level + off-axis feel stays a visual gate (Safari/Chrome). A descending-stair / forced-down-ramp robustness gap (a high→low `route` emits an empty/unwalkable stair connector, and a forced steep descending ramp builds instead of throwing) was noted for 2.2.5 (resolved in Slice 2.2.5a below — see the `route` hardening). **Epic 2 · Slice 2.2.5a ("World Graph & Collision-Aware Placement") then landed (2026-07-02):** the world is now a **graph the placer solves**, not hand-tuned world positions. `packages/dungeon/src/aabb.ts` adds the `Aabb` contract (`aabbOfBoxes`/`aabbUnion`/`aabbIntersects`/`aabbContains`/`aabbIntersection`/`transformAabb`); `region.ts`'s `RegionData` gains a required `bounds: Aabb` — the placer's piece-vs-piece broad-phase unit, which `connect.ts placePiece` now transforms (conservative yaw-rotate + translate) alongside meshes/colliders/connections/instances — and `RegionKind` gains `"authored"` (the pinned hand-built level, participating in placement as an obstacle rather than a theme). The placer, `packages/dungeon/src/layout.ts` `layoutWorld(graph, seed)`, is a deterministic collision-aware incremental graph embedding in the Ma-2014/Edgar shape: pinned nodes seat first and stay frozen, then a most-constrained-first order (most already-placed neighbours first, ties broken by BFS depth from the pins, then id) places the rest one at a time by sampling candidate seatings — `join`-mating the node's portal to its already-placed parent portal at a seeded grid of connector lengths × yaw offsets (0°, ±15°, ±30°, ±45°) — hard-accepting the first candidate that clears the occupancy rules AND makes every now-both-placed edge (its own tree-seating edge, plus any loop-closing edges to already-placed neighbours) realizable via `route`, with bounded backtracking (a stack of revisable frames, a total-attempt cap) before throwing setup-loud with a per-node candidate-failure histogram. **Force-directed relaxation was research-overturned** as the placement foundation — no shipped system was found embedding a world graph by global force-directed relaxation; see `docs/research/2026-07-01-dungeon-2.2.5-layout-placement-and-megastructure.md` — in favour of this deterministic incremental embedder. Loop-closing edges never drive the candidate search that picks a node's position; they are only checked (and can trigger backtracking) once the tree edge has already placed the node. `layoutWorld` is pure: no GPU, no Rapier, no wall-clock. The collision model, `packages/dungeon/src/occupancy.ts`, tracks three obstacle classes — placed-piece **envelopes** (must never interpenetrate each other; exact touch is legal), placed-piece **solids** (conservative cuboid AABBs, or — for cave voxel proxies — exact per-cell membership, since the whole-grid AABB is mostly air and would false-reject any cave-mouth connector), and committed-connector **clearance volumes** (reserved walking air, segmented along a climb rather than one giant box) — checked pairwise across 5 acceptance rules (piece-vs-piece envelopes; piece envelopes vs. committed clearance air; clearance air vs. piece solids outside a **portal exemption** — a box around each portal a connector is allowed to bore through its own endpoints' walls near the seam, so only deeper clips reject; clearance air vs. a non-endpoint piece's envelope; and clearance air vs. another committed connector's air or its floor slab). `connect.ts route` was hardened for the new direction-agnostic caller: descending stair-runs now emit a real mirrored staircase instead of a `dh<0` empty step loop, and the ramp slope-limit guard now tests `Math.abs(pitch)` so a forced steep DESCENDING ramp throws setup-loud like its ascending counterpart — resolving the carried `connect-route-descending-stairs-empty` backlog. `themes/box-room.ts` generalizes `BoxRoomParams.door` (one `DoorSpec`) to `doors: DoorSpec[]` (≥1, at most one per cardinal side, setup-loud on violation) so rooms of degree > 1 exist for a graph world; `pillarHall` exposes this as an optional param (default = the historical single S door), `greatHall` stays single-door. The world-graph contract, `packages/dungeon/src/world-graph.ts`: nodes are region instances (a local-frame `RegionData` + optional frozen `pinned` placement), edges are connections between two node portals — verticality (`heightDelta`) and the reserved-for-later `directed`/`requiredVerbs` fields live on EDGES, not nodes; `validateGraph` throws setup-loud on dangling/duplicate/double-used portals, on any `directed`/non-`"walk"` edge (no fall verbs yet), and on a disconnected graph — connectivity means reachable-from-pins by BFS (a lone pinned obstacle with no edges is legally its own root). The proof graph, `packages/dungeon/src/world.ts` `buildWorldGraph`, pins the authored level as a **collision phantom** — empty meshes/instances but real `LEVEL_BOXES` colliders + bounds, so the placer treats it as a first-class obstacle while `main.ts` still realizes its visuals itself — plus a branching cave wing, two ground halls off the cave's east/north mouths (resolved by facing, not index), two elevated `pillarHall`s (one up a ramp, one up forced stairs, height-stacked to fit the same footprint), and a two-hop descending cycle: `upperA → landing` (a forced-stairs descent) then `landing → hallA` (a flat closing corridor), where `landing` is a new small room seated in the open void east of the authored level's wall (its stair-seat door is built full-height to avoid the descending connector's headroom clipping a normal lintel — the general case is tracked by the new `descending-connector-lintel-clip` backlog). **Design history, recorded honestly:** the original spec called for a direct `upperA ↔ hallA` descending loop; that proved geometrically infeasible during execution — a straight descending connector between the chamber-elevated `upperA` and the cave-seated `hallA` would have to cross the authored east wall / cave geometry and mate two far-apart, mis-angled doors — so it was redesigned (user-approved) to route both hops through the void `landing` room instead, keeping both hops clear of the authored wall while still exercising the descending-stairs path end-to-end. `compose.ts` is **deleted entirely** (`buildArea`/`attachWing`/`attachUpperRoom`/`attachUpperLevel`/`placeRoom` and all its GATE-TUNE world positions); `main.ts` now renders `layoutWorld(buildWorldGraph(WORLD_SEED))` directly — no hand-tuned world positions for the graph-placed content (the pre-existing baked cavern, seated at its authored `[0,0,-24]` outside the world graph, is unchanged). **No `@furnace/core` change.** Headless: `aabb`/`occupancy`/`world-graph`/`layout`/`box-room`/`connect` unit tests (incl. layout backtracking + an impossible-graph diagnostic-throw case) and `world.test.ts` (zero envelope overlap, determinism, the descending hop's >4 m drop, the closing hop's flat portal-Y check); GPU walks in `area-traversal.gpu.test.ts` (the cave→room seam, the ramp climb to `upperA`, the forced-stairs climb to `upperB`, and the ~10 m loop descent from `upperA` to `landing`) and the re-anchored `traversal.gpu.test.ts` (cave-hub fuzz, level↔wing seam, no-fall-through). Placement/theme feel stays a visual gate (Safari/Chrome). Open follow-ups: two backlog entries surfaced during execution (`docs/backlog/dungeon/descending-connector-lintel-clip.md`, `docs/backlog/dungeon/occupancy-piece-vs-connector-slab.md`); `docs/backlog/dungeon/connector-geometry-stitching.md` (untouched — surface stitching remains a separate gap); force-directed relaxation as a possible future *aesthetic* pass over an already-valid placement; and Slice 2.2.5b, which replaces this file's hand-authored topology with a generated graph. **Epic 2 · Slice 2.2.5b Phase A ("Connector enclosure") then landed (2026-07-03):** every routed connector is now an **enclosed tube** — side walls + ceiling with portal-shaped open ends — with a per-edge `WorldEdge.enclosure?: "open"` style (guardrail-height walls, no ceiling; absent = tube), killing the 2.2.5a-gate "bare floor strip through void" seam look. The enclosure is built in `connect.ts` as **vertical-walled rings quantized along the climb** (`enclosureBoxes`, `RING_RISE 0.25 < CEIL_T 0.3` so adjacent ring ceilings always overlap — sealed by construction; pitched slabs were rejected because their end faces lean along-climb and can't end flush against a vertical door plane); floors are unchanged (corridor slab / pitched ramp slab / `stepBoxes`), all enclosure boxes are both meshes and cuboid colliders (tubes = walls you can't fall past; `open` = real guardrails), and end treatment follows portal kind — flush at `door` planes (box-room portals sit at wall-centre, so ends bury half a wall-thickness — sealed by construction), `SEAM_OVERLAP`-embedded into the rock at `tunnel-mouth`s. A new exported `connectorSection(from, to)` single-sources the cross-section (outer width = max width + 2·`SHOULDER`, headroom = max height) between `connect.ts` geometry and `layout.ts` clearance math; `clearanceBoxes` (now exported — the containment contract is unit-tested: enclosure ⊆ grown clearance) pads tops by `ENCLOSURE_TOP_PAD = CEIL_T + RING_RISE`, and `portalExemption`'s vertical reach rose to match (without it the grown clearance sliver above the old exemption top sits in solid rock at every cave mouth → false-rejects). Placement is **style-blind** (`world.test.ts` unchanged except one authorized floor-only-proxy repoint). `world.ts` marks the `upperA → landing` descent `open` so the gate exercises both styles. **Post-gate fix (the user fell through a hole):** stair floors never carried seam aprons — treads cover `[td/2, run+td/2]`, so the descending mirror left a 0.44 m full-doorway gap over void at the landing door; `floorBoxes` now emits flat **end aprons** extending `SEAM_OVERLAP` past both portals like every other floor, and the new PURE `tests/world-seams.test.ts` sweeps every edge's walk line (extended into the rooms, lateral fan, exact oriented-box tops + voxel columns) asserting **no capsule-scale unsupported disc** — empirically verified to catch the pre-fix hole (sub-capsule slivers tolerated: the oblique-join threshold wedge is `docs/backlog/dungeon/oblique-join-threshold-wedge.md`, folded into Phase B thresholds). No `@furnace/core` change. Headless: enclosure emission/seal/containment/end-treatment + `connectorSection` unit tests, the stair-apron threshold test, the world seam sweep, GPU enclosure ray probes (`connect.gpu.test.ts` — tube ceiling blocks an upward cast, `open` doesn't, rails block sideways) + all traversal walks now run INSIDE tubes; look/feel gated visually (Safari/Chrome, both styles). **Epic 2 · Slice 2.2.5b Phase B1 ("Built interfaces") then landed (2026-07-04):** Phase B was split (user decision) into B1 (built interfaces, this slice) + B2 (the topology generator, next); every seam is now **built↔built**, via three mechanisms + setup-loud doctrine enforcement. **(1) Mouth collars:** a new `packages/dungeon/src/built.ts` kit — `mouthCollar(mouth, {opening, envelope})` → 4 oversized masonry boxes (jambs/lintel/sill forming a sleeve `COLLAR_EMBED 0.8 + COLLAR_PROUD 0.4` deep) whose edges bury into the rock by interpenetration (no CSG; the mining portal/collar idiom) — applied by `themes/cave.ts` to EVERY raw mouth (branches + the entrance): the cave's `connections` are now the collars' standardized `door`-class portals (2.0×2.8) at collar **mid-depth** (the "portal sits at the centre of its wall's thickness" convention → flush tube ends bury half the collar depth, sealed by construction); masonry material matches the connector family; bounds grow via `aabbUnion`. **(2) Descent-arrival landings:** `connect.ts` exports the single-sourced walk profile `walkLineAt(dh, run, z)` consumed by `floorBoxes`, `enclosureBoxes` AND `layout.ts clearanceBoxes` (extending the Phase-A containment contract — geometry and the placer's reserved air cannot drift): a DESCENDING connector flattens for `LANDING_LEN = 2.0` m before its lower `to` portal (ramp landing slab; stairs low apron generalized into the landing; enclosure ring count follows the climb-window slope), and `LANDING_LEN >= CLEARANCE_SEGMENT` (now exported; unit-asserted) makes the arrival clearance segment flat by construction — so a NORMAL door works at a descent foot: `world.ts`'s full-height `LANDING_SEAT_DOOR` hack is retired (height 5→2.8, placed clean, no GATE-TUNE) and `descending-connector-lintel-clip.md` is resolved+deleted. **Design history, recorded honestly — two mid-execution revisions (both verified-GPU-wedge-driven, user-authorized):** the spec's original symmetric low-end landing steepened ascending climbs and was overturned — landings went DIRECTIONAL (ascending = linear, no landing: its low end is a free-floor departure; the true discriminator is a WALLED low portal → `docs/backlog/dungeon/landing-walled-low-portal-discriminator.md`: B2 must orient walled-low-portal climbing edges as descents, or promote walledness into the portal contract); and threshold plates went ROOM-SIDE-only after the original portal-centered plate overhung upperB's steep treads (sub-capsule headroom wedge). **(3) Threshold plates:** every connector end emits a door-aligned plate (`width + 2·0.3` × `THRESH_DEPTH 0.6` reaching from the portal plane INTO the room along the TRUE signed local facing, top `THRESH_LIP 0.015` proud — the real-threshold profile, kills coplanar z-fighting) covering the receding room-floor jamb wedge at ANY join angle (54° repro unit-tested); the connector-side jamb triangles were already covered by the floors' `SEAM_OVERLAP` overshoot — `oblique-join-threshold-wedge.md` resolved+deleted. **Doctrine:** `route` throws unless BOTH ends are `door`-class and `validateGraph` rejects non-door edge portals; `Connection.kind` keeps `"tunnel-mouth"` as the raw-organic pre-collar marker; the tunnel-mouth embed end-treatment (`EndKinds`) is DELETED — every enclosure end is flush. Research-backed: `docs/research/2026-07-03-dungeon-2.2.5b-b1-built-interfaces.md` (3-lane cited pass — Skyrim kit transition pieces / Warframe door fittings / mining portal+collar vocabulary; IBC/OSHA/ADA landing codes bracket `LANDING_LEN`; real thresholds sized to wall depth + proud lip). New backlog: the walled-low-portal discriminator (above) and `charactermover-ramp-mount-threshold.md` (empirical: a 47.2° ramp mounts from flat ground, 49.64° stalls — `chooseKind`'s 52° ramp band exceeded the practical mount ceiling; resolved+deleted at the 2026-07-06 Epic 2 closure — the 45° `RAMP_MOUNT_LIMIT_RAD` ceiling in `walkability.ts` now caps `chooseKind` below the mount limit); `region-connection-algorithm-refinement.md` narrowed to the cave branch-direction constraint; `connector-geometry-stitching.md` now covers only true isosurface↔built rim blending (Epic-3; the SDF-stamp option noted). **No `@furnace/core` change.** Headless: `tests/built.test.ts` (envelope masking, opening-prism clear, yawed facing, mid-depth door), `connect.test.ts` (directional `walkLineAt`, landing floors both kinds, plates + the 54° oblique repro, doctrine throw, seal + containment with landings), `layout.test.ts` (clearance follows `walkLineAt`; the `LANDING_LEN` invariant), `world-graph.test.ts` (door-kind validation), `cave.test.ts` (door-presenting contract), a GPU collar probe (`connect.gpu.test.ts` — lintel/jamb block, the opening passes) and all traversal walks now cross collared mouths; `tests/world-seams.test.ts`'s walk-height model repointed to `walkLineAt` (authorized — hole-detection unchanged; profile correctness owned by unit tests); `world.test.ts` untouched. Visual gate passed (Safari/Chrome). Process note: reviewer subagents must inspect history via `git show <sha>:<file>`, never `git checkout` (a detached-HEAD incident mid-slice). **EPIC 2 CLOSED (2026-07-06)** at its shipped extent (2.0 → 2.2.5b-B1). The B2/B2c placement arc (topology generator + toolbox placer: `topology.ts`, rebuilt `layout.ts`, `locus.ts`, `chains.ts`, exact-OBB `aabb.ts`, `built.ts` caps) hit a placement wall at ≥90%-first-try bars — four verified hard blocks, root-caused to substrate mismatch (rigid-tile placement forced onto field-native caves); see `docs/learnings/2026-07-05-dungeon-placement-arc-postmortem.md` (process rules → `.claude/rules/working-standards.md` §Planning) and `docs/learnings/2026-07-04-dungeon-2.2.5b-b2-placement-wall.md` (evidence). The generator+placer merged as a LIBRARY the cockpit consumes; the hand upper-level showcase (upperA/upperB/landing) was retired at closure (walkable-XOR-placeable edge); `main.ts` still ships the hand ground world. **EPIC 3 = the Generation Cockpit** (spec'd 2026-07-06; research: `docs/research/2026-07-06-epic3-cockpit-workflow-prior-art.md`, `...-generator-entity-precedent.md`, `docs/research/2026-07-06-editor-cockpit-audit.md`): the editor generates, curates, and BAKES the world; the runtime loads baked sections plus expands generator-entity sockets. **Doctrine: editor-time generation may use search-class algorithms — a human with reroll, caps, and curation tools absorbs failure; runtime generation is restricted to construction-guaranteed or degrade-never-fail vocabularies via generator entities, and the guarantee class is an explicit setup-loud field on the socket contract.** Ladder: 3.0 foundations (editor-openable dungeon, layout budgets, watch loop, probes) → 3.1 the loop (generate→reroll→freeze→bake→walk) → 3.2 seeing & curating (spatial failure viz, chrome rework, local curation verbs) → 3.3 generator entities (maze first) → 3.4 organic arc ("built places, organic carves", carve-union probe first). Old Epic 3 (streaming/deep gen/LLM) renumbers to Epic 4. **Epic 3 · Slice 3.0 ("Foundations") then landed (2026-07-06):** the dungeon is now **editor-openable** — a `furnace.config.json` + `src/editor-extensions.ts` (the cockpit seam re-exporting the generator import graph — `buildWorld`/`layoutWorld`/`generateWorldGraph`/`realizeRegion` — proven browser-bundlable from the dungeon root by the editor-side P3 bundling smoke test); `bun run edit` in `packages/dungeon` opens the editor daemon on the dungeon. Core placement search is now **bounded** — `LayoutBudget` + `DEFAULT_LAYOUT_BUDGET` (postmortem "search systems get budgets on day one"; fail-fast, defaults byte-identical to the old hardcoded constants) thread through `layoutWorld` and a new optional `buildWorld(seed, config?, budget?)` param. Premise probes P1 (retry-backed placement rate + p95 wall-clock, `scripts/measure-b2c.ts --retry`), P2 (browser generate+realize timing), P3 (the bundling seam) ran, and a headless **generate→bake→load round-trip** (`scripts/bake-generated-wing.ts` + committed `generated-wing.fmesh`/`.scene.json` fixtures + `tests/generated-bake-roundtrip.gpu.test.ts`, reusing the 2.1 persistence machinery) is the 3.0 gate artifact. **No `@furnace/core` change.** **Epic 3 · Slice 3.1 ("The Loop") then landed (2026-07-06):** the cockpit loop is real — **generate → reroll → freeze & bake → walk**, gated live (Safari/Chrome). Dungeon side: `worldAttempts()` iterator in `world.ts` is the SINGLE owner of retry policy + `seed:k` derivation (`buildWorld` drains it byte-identically; the cockpit steps it between paints, cancellable between attempts) plus `COCKPIT_CONFIG`/`COCKPIT_BUDGET` (the P1 probe chose the **tight** tier — rate 100% @6 rooms × 12 derived-seed attempts, p50 ≈ 1.2 s, tail 20–73 s ∝ per-draw give-up cost, accepted behind progress+cancel; the ultra tier failed the firstShot bar). `bake.ts bakeWing` (PURE, returns a file set): render-only region/connector scene docs + `.fmesh` sidecars + `manifest.json` carrying provenance {winning derived seed, config, budget}, per-region `placement`, cuboid collider lists (voxels never serialize), and **`themeParams`** — the materializer's exact extra generator params (cave `mouths`/`capped`, pillarHall's graph-derived `doors`), built ONCE in `topology.ts materializeNode` and recorded on `WorldNode.themeParams` so record and region cannot drift. A bare `{theme,seed,origin}` re-run reproduces a DIFFERENT region — found LIVE at the gate as scatter-in-doorways ("blocked rooms": dropped multi-door specs shifted scatter keep-outs) and guarded since by the placement-LEVEL parity test `bake-dressing-parity.test.ts` (full cuboid/proxy/dressing byte-parity on the gate's own seed; the earlier count-level assertion had passed while placements differed). `wing-loader.ts`: manifest present → `main.ts` skips live generation and fragment-loads the baked docs, creates static bodies from manifest cuboids, regenerates each cave's voxel proxy via the new `caveProxy` (field-only, no meshing) and re-expands dressing via the new `caveDressing` (scatter over the DECODED baked mesh — sound because core `rng.derive` is state-independent) / cheap box-theme re-runs, all `placePiece`-seated; manifest absent → the live path, unchanged. **Pr-2 (cross-engine determinism) FAILED and the recorded fallback shipped:** JSC vs V8 diverge on transcendental `Math` in `placePiece` yaw + the placer's accept/reject flips (local-frame mesh/scatter/voxel bytes are identical) → **the browser bakes** (reproducing its own preview exactly) and uploads the file set; the daemon's `generation.bake` only validates root-containment (all paths checked BEFORE any write; dotfiles refused) + writes + emits `generation-baked` SSE — the editor carries zero generator knowledge (`docs/learnings/2026-07-06-cross-engine-placement-determinism.md`: never regenerate placement cross-engine). Editor side (see the `packages/editor` bullet). Gate verdict, recorded honestly: the loop works end to end; walk-quality findings are GENERATOR quality (verified by the parity split, not bake fidelity) — vertical cave joins + tight corners → `docs/backlog/dungeon/generated-wing-traversal-quality.md`; cockpit UX gaps (unclamped knobs, per-attempt UI blocking, unmeasured >6-room envelope, between-attempts-only cancel) + the **DECIDED single-`wing.scene.json` consolidation** (today's ~27-file bake floods the scene list; also deviated from the spec's single connectors doc) → `docs/backlog/editor-and-tooling/generation-cockpit-ux-gate-findings.md`, all owned by 3.2's chrome/editor full pass (user verdict: the editor needs A LOT of work — that IS 3.2). Baked output (`regions/generated-wing/`) is gitignored + biome-ignored user content. Suite 1300/1 skip/0 fail. **No `@furnace/core` change.** **Epic 3 · Slice 3.1 ("the Loop") then landed (2026-07-06):** the dungeon gained the browser-side generation cockpit surface — `worldAttempts` (a stepped per-attempt placement iterator driving the editor's preview loop), `bakeWing` (`bake.ts` — pure, FS-free wing writer: render-only docs + `.fmesh` sidecars + a manifest), `wing-loader.ts` (fragment-loads a baked wing at the chamber door, re-expanding the cave voxel proxy + dressing from provenance at load), and `caveProxy`/`caveDressing` (those runtime re-expansion helpers) — all reached by the editor through the `editor-extensions.ts` cockpit seam. A Pr-2 determinism probe (`scripts/hash-world.ts`) found `buildWorld` placement diverges bun/JSC vs node/V8 (transcendental `Math` in the `placePiece` join-yaw transform + the placement search), so bake runs **in the browser** (browser-uploads-payload fallback), never the daemon (`docs/learnings/2026-07-06-cross-engine-placement-determinism.md`). *(Slice 3.1 seal — user finalizes at slice close.)* **No `@furnace/core` change.**

### packages/editor (bullet as of the extraction)

- `packages/editor/` (`@furnace/editor`, private) — the editor (M4 command layer + M5A inspector + M5B viewport interaction landed). A Node-portable daemon (no `Bun.*` in `src/`, enforced by `tests/no-bun-leakage.test.ts`) that serves the editor frontend same-origin and owns a zod-validated command registry over `POST /api/<command>`: a mutable document session (open/save, transactional registry-validated mutations, snapshot undo/redo), an SSE change feed (`GET /api/events`), and scene-file watching with conflict semantics. It esbuild-bundles the consumer's `@furnace/core` + extensions twice — a browser engine bundle and a node-platform registry bundle for daemon-side validation (project-first resolution — the editor contains no engine). React 19 + dockview chrome (prebuilt to `dist/frontend`, served by the daemon); dogfood: `bun run edit` in `packages/hello-world`. M5A added an editable, reflection-driven inspector (component + settings + resource editing with live preview), multi-entity selection (replace/toggle/range, modifier-click), and the `scene.batch` command (atomic multi-entity edit, one undo entry). M5B added GPU-id viewport picking, AABB selection highlight, a translate gizmo (3-axis drag, anchor-relative-absolute, multi-select one-undo), an editor orbit/pan/zoom camera (init from scene cam, never serialized), NumberField drag-scrub (Safari-safe Pointer Events, Shift=fine), focused-input echo-guard, and `revertSettings`. The consumer's `@furnace/core` scene loader (which the editor renders, owning no engine itself) now instantiates the **full built-in set** from data — geometry cube/sphere/cylinder/plane, shaders unlit/lit/texturedLit/textured/normalColor, textures checkerboard/load, standard material (+texture/sampler), effects bloom/tonemap, light directional/point/spot, rigidBody static/dynamic — plus **physics-from-data** (lazy world + rigidMesh composite, instantiated but **NOT stepped** — a consumer's game loop drives stepping). The editor viewport renders scene lights+ambient (the lit-viewport payoff) and defers the post chain (`effects: []`, non-HDR context — fidelity, not a crash). AI bindings (MCP mount, viewport capture, embedded/outbound LLM) are deferred to `docs/backlog/editor-and-tooling/editor-ai-integration-milestone.md`. **Slice 3.0** added a daemon inner-loop watch: it now watches the consumer's extensions directory and emits a `bundle-outdated` SSE event so the browser reloads on source change (closing the cockpit inner-loop's browser-engine-bundle staleness gap), and the dungeon is editor-openable (`bun run edit` in `packages/dungeon`). **Slice 3.1 ("the Loop")** added the generation cockpit: an engine-bundle-side **preview host** (`createPreviewHost` — HDR+fog+orbit, generation-session-only), the browser bundle's `extensions`-namespace generator seam, a dockview **Generation panel** + ephemeral session (stepped `worldAttempts` preview → reroll → freeze&bake), the **`generation.bake`** command, camera-less **fragment-doc opening** in the viewport host, and session concurrent-open race hardening + registry `invalidate()` on the extensions-dir watch. The bake uses the **browser-uploads-payload** fallback — the browser bakes the wing in its own engine and uploads the file set, the daemon just validates+writes+emits `generation-baked` — adopted because a Pr-2 probe found world placement diverges bun/JSC vs node/V8 (`docs/learnings/2026-07-06-cross-engine-placement-determinism.md`). Gate hardening (found live, 2026-07-06): both viewport canvases stay LAID OUT at all times (visibility swap, never `display:none` — a zero-size canvas throws in `bindToCanvas` at host init); **fog is a viewport VIEW-FLAG** (`PreviewHost.setFog`, default OFF — game-density fog at orbit distance obscures the structure the cockpit exists to show; the toggle lives on the viewport overlay, the UE/Unity show-flags pattern, seed of 3.2's view-flags block); freeze reads only the `done`-status config SNAPSHOT (knob edits invalidate the preview — freeze bakes exactly what is on screen). Gate-found cockpit UX gaps + the DECIDED single-`wing.scene.json` bake consolidation are 3.2 inputs: `docs/backlog/editor-and-tooling/generation-cockpit-ux-gate-findings.md`. Architecture: `docs/reference/editor-architecture.md`.

