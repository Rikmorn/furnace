# Epic 3 · One Field F3b — the cave & the entities

- **Sealed:** 2026-07-25
- **Package(s):** core, dungeon, editor
- **Gate:** user-gated Safari, one gate round + fix round, accepted with variances
- **Suite:** 2076 pass / 1 skip / 0 fail

**One Field F3b ("the cave & the entities") SEALED 2026-07-25, user-gated Safari (one gate round + fix round, accepted with variances):** the full phase sentence shipped — stamp + cave a world, scatter props, reconfigure, freeze/bake, bake the artifact, walk it. Core: the **cave generator** (skeleton-then-stamp: chambers + inductively-connected passages + EXPLICIT verticality edge types with 0.25 m-quantized protected stepped floors; mined/organic/mixed themes; integer-hash noise, no transcendentals; emits patch ops per D-F3-12), the **scatter generator** (first `contextFree: false` def — deterministic surface projection to EXPLICIT placement records; re-cook against a pre-span scratch, prop drift via record AABBs), `GeneratorResult {ops, placements}` + `EvaluateContext` widening, the placement op + oplog v3, the placement artifact in `bakeFieldWorld`, the kit-render extraction to `core/field/kit-render.ts` (third-consumer trigger), the `capsule` BrushShape. Dungeon: `catalog/entities.json` + baked `.fmesh` archetype meshes (net-new procedural rock/stalagmite generation), placement loading (instanced render + colliders DERIVED from catalog primitives, capsule added), the P-F3-1 probe (STRONG envelope — 48/48 passage + 12/12 composite lanes walked, all failure classes zero; report committed `docs/research/2026-07-23-f3b-p-f3-1-stepped-floor-probe.md`). Editor: scatter authoring (catalog-seeded params, oriented placement ghosts, instanced prop proxies), entities-list prop rows, the void-cast X-ray, the segment brush (two-click swept capsule), five past-due viewport MIGRATION markers retired, `packages/core/CLAUDE.md` drafted. Premises: P-F3-3 within napkin (108 chunks / 442 KB / ~100 ms); P-F3-4 confirmed. Suite 1890 → 2076/1/0 across the two tranches + fix round; biome held at baseline (the +46 root-tranche test warnings have their own hygiene-phase entry). **Gate round 1 found TWO mechanism bugs, both pre-figured by the repo's own records:** scatter STARVED on caves (3 placements on a whole default cave — quantized floors land density EXACTLY 0 on a sample; the strict sign-flip scan was blind; the pre-filed blind-spot backlog entry fired AT THE GATE — grep-backlog-first again) → zero-tolerant crossings + rock-evidence guard (`4aa4690e`, 3→11 = honest vs the 71 m² carved footprint; preview↔commit parity probe-proven byte-identical); and EVERY host refusal (void-cast budget/empty/busy, "select a region first") rendered invisibly on the footer status line — two independent "feature seems dead" reports, one root cause → destructive tone (`10551f9e`). **Accepted variances:** the void cast's pixels remain visually UNCONFIRMED (noted in editor-architecture §18); gate steps 7–8 not re-driven after the fix round (the headless GPU walk-gate tests cover the dungeon path, props colliding included); default-size rocks are STEPPED OVER by design tension (half-buried anchoring vs mover step-up — filed then as `placement-props-stepped-over.md`, superseded at F4 by `docs/backlog/engine-architecture/catalog-collision-escalation.md`). **Sequencing decision (user):** the accumulated gate-UX sets (f2b 1–6, f3a, f3b, interaction-model-redesign) batch into a DEDICATED UX/polish stage after the feature phases — F4 continues the feature set without breaking focus. Backlog: f3b gate-ux set + cave web-topology filed; scatter-empty-policy, two-point-tunnel, viewport-MIGRATION, kit-render-duplication, per-package-claude-md, scatter-blind-spot all resolved/deleted; visual-polish-pass shed its scatter-archetype section (resolved into the catalog bake).
