# Epic 3 · One Field F3a — smart objects

- **Sealed:** 2026-07-23
- **Package(s):** core, editor
- **Gate:** user-gated Safari, one gate round
- **Suite:** 1890 pass / 1 skip / 0 fail

**One Field F3a ("smart objects") SEALED 2026-07-23, user-gated Safari (one gate round):** committed generators became reconfigurable smart objects — reopen from the entities list → edit params/seed/region → ghost → Apply → in-place span-splice replay with a drifted/orphaned report → one ⌘Z restores exactly; freeze/bake verbs; the patch op + verified semantic compaction; stamp rotation + door-offset authoring + selection-derived size defaults; the op-cost meter. **Core** (`@furnace/core/field`): discriminated `LogEntry` union (ops/splice/entity-update) with image-based LIFO undo/redo (the F2b tail invariant restated — splice restores positionally); `PatchOp` (masked absolute cell writes — the compaction primitive and F3b's cave emission form); `reconfigureGenerator` (bounded-influence affected-set culling, flood-masked ops conservatively always-intersecting, drift = outcome diff / orphan = zero delta, no mutation on throw); `setGeneratorFrozen`/`bakeGeneratorEntity`; `compactRuns` (fold-verified byte-exact BEFORE discard, refuses live spans/smooth/selection masks, **requires a quiescent history** — undo entries address ops by position; durable anchor fix backlogged) + `logStats`; chunk-keyed snapshot records with a REDESIGNED restore fast-path (the plan's records-only seeding measured at par with baseline — the executor rebuilt it as per-chunk culled replay with pristine fallback: 4.19× with zero records, reviewer-reproduced); oplog v2 envelope (base64 patch payloads, v1 bare-array compat, EVERY wire string union closed exhaustively-both-directions after a three-round review found silent-divergence modes — bogus mask kinds wrote 0 cells silently); `MAZE_PITCH_CELLS` public. **Editor:** reconfigure sessions on the F2b stamp-session machinery (policy not recorded in provenance — opens at "replace", documented), freeze/bake row verbs (bake confirms via the App-owned dialog), drift list + `frameChunks` click-to-frame, meter line under a log-signature dedup, load-time-only compaction (the quiescence guard), `FieldHost.undo()/redo()` + the canvas-⌘Z scene-undo leak fixed (pre-existing), `deriveSizeDefaults` (the plan's maze fit formula was WRONG — corrected from the real footprint with an overflow-proving test). **Process:** P-F3-2 probe first (full-prefix replay 310–318 ms at 75% → GO-FASTPATH by 3.2×); two executor sessions (core tranche 13 commits, editor tranche 10) subagent-driven with independent verification here between and after; the D-F3-7 spec amendment (snapshot existence bound to the measurement) recorded transparently. **Gate round 1 fixes:** ⌘Z now clears the standing drift report (stale by construction); the entity highlight boxes the stamped FOOTPRINT (`generatorFootprint`, patch-aware) instead of the recorded selection region, which routinely over-drew. Deferred AS A SET → `docs/backlog/editor-and-tooling/field-f3a-gate-ux-findings.md` (mouse-driven region move, in-viewport pointer/select tool, box/wand selection feel — F4 recharter with the F2b set; F2b item 7 RESOLVED by this slice). Suite 1693→1890/1/0; typecheck clean; zero new lint warnings (91 == master). Merged FF to master; branch kept. NEXT: F3b "the cave & the entities" (spec §3; P-F3-1 stepped-floor probe is its first task — probe visibility over algorithmic guarantee per the user's curation-first stance).
