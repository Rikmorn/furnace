---
summary: Epic 3 · recharter + Slice 3.3 W1 — World model, field-only — also carries W2 (substrate + grid-built halls) and W3 (maze + World-panel assembly, the phase gate)
sealed: 2026-07-11 → 2026-07-13
seq: 18
---

# Epic 3 · recharter + Slice 3.3 W1 — World model, field-only

- **Sealed:** 2026-07-11 → 2026-07-13
- **Package(s):** dungeon, editor
- **Gate:** recharter (2026-07-11) — not a gate event, charter only; W1 (2026-07-11) — user-gated Safari + editor loop; W2 (2026-07-12) — user-gated Safari, two gate rounds; W3 (2026-07-13) — gate passed live in Safari
- **Suite:** W1 — 1489 pass / 1 skip / 0 fail; W2 — 1553 pass / 1 skip / 0 fail; W3 — 1597 pass / 1 skip / 0 fail

**Epic 3 recharted as Slice 3.3 "Worlds" (2026-07-11), closing 3.2:** 3.2.1–3.2.3 shipped; the chartered 3.2.4 (seeing) / 3.2.5 (curating) RENUMBER to **3.4 / 3.5** — they execute AFTER 3.3, rebuilt against the world model; old-3.3 (generator entities) redistributed (maze vocabulary → 3.3 W3; socket contract + guarantee class → **3.6 JIT regions**, backlogged); old-3.4 (organic arc) absorbed into the connector-carve work (isosurface↔built blending stays behind Jolt). Grounding: the substrate research + spike (`docs/research/2026-07-10-world-substrate-and-region-composition.md`, `…2026-07-11-voxel-substrate-spike-findings.md` — S2 validated, P1–P4/P5-read/E1–E3 all user-gated). Charter decisions: gate = editor-assembled multi-region world ON the substrate walked in-game; CLEAN CUT (mesh generators + free-space placer + mesh connector kit retire in-phase — the measured 12-room ceiling is a free-space-embedding property, not a graph property); standards move grid-friendly (door 2.0×3.0, wall 0.5, pillar 0.5/1.0); one global lattice / per-region storage; disjoint region volumes + connectors; Jolt NON-blocking (gate connectors constructive; no free-form breach verb until the Jolt phase).

**W1 ("world model, field-only") landed (2026-07-11, user-gated Safari + editor loop):** wings → WORLDS end-to-end with the existing cave class only — **no `@furnace/core` change**. Executed subagent-driven (11 tasks + post-gate feedback commit; 14 commits off `1da449f` on `epic3-w1-world-model`, LOCAL; verified independently: **1489 pass / 1 skip / 0 fail**, probe 4 tests / 8,630 assertions green in isolation, typecheck + check clean). Landed: `world-spec.ts` (`WorldSpec` — declarative regions/connectors/`startRegion`, connectivity validation, `DEFAULT_WORLD` two-caves+tunnel with join-DERIVED placement for the b-end — search-free); `connector.ts organicTunnel` (the connector OWNS its volume: lattice-aligned grid, floor-routed capsule bore, burial seams, grid CLIPPED at door planes so bore end-caps never voxelize inside cave air — locking test); `world-build.ts realizeWorldSpec` (deterministic placements + computed `playerStart`); `bake.ts bakeWorld` (ONE merged `world.scene.json` + `.fmesh` + `world.json` manifest LAST under `worlds/<name>/`; byte-deterministic re-bake — no `bakedAt`; per-connector re-expansion inputs in the manifest); worlds-index + `/worlds` route + committed `worlds/default/` fixtures (fresh clones boot; bun-baked — sound because placement BAKES, only local-frame ops re-expand); `world-loader.ts` (setup-loud on missing index/manifest — the live-generation fallback retired with wings; placement-level dressing parity guarded; post-gate load banner: world name + region seeds = the bake's identity); `main.ts` boots solely via `loadWorld` (hand level + wing path OUT; modules in-tree until W4); editor world flow (worker `runWorld`/`bakeWorld`, deterministic no-attempts response; daemon was already destination-agnostic — no-op + test). **THE PROBE (binding, fail-fast) PASSED after one legitimate boundary iteration:** the full-collider-set walk (A→tunnel→B, reverse, wall-hug lanes) found a real seam wedge — the 0.95 connector bore overlapping the 1.6 cave bore collapses walkable air to the INTERSECTION (voxel ceiling pinch) → `TUNNEL_RADIUS` matched to 1.6; teeth verified (revert → all lanes fail); durable fix (shared-lattice/carve-union composition) backlogged (`world-connector-bore-and-overlap.md`; also `connector-boreaxis-float-dust.md`, W2 trigger). Executor plan-corrections validated at review: phantom-target facing (keep `pA.facing` → portals face each other, `dot=−1` — the plan's sketch was wrong, its own test was right: TDD worked), connector grid door-plane clipping (above). Gate notes: rerolls produce similar worlds BY DESIGN (fixed W1 macro shape, seeds vary interiors — variety is W2/W3's); "which bake am I walking" gap → the load banner (proper UI = W3 assembly UX). Backlog: `jit-runtime-regions.md` filed (3.6 seed); `worlds-index-manifest.md` resolved + deleted. Reference docs updated at seal (`dungeon-architecture.md` §2 + §5b, `editor-architecture.md` §13.7, AGENTS.md dungeon bullet — game/bake/ladder clauses). LOCAL on `epic3-w1-world-model` (unmerged — user merges/pushes after own check). NEXT: W2 (substrate core + grid-built halls) brainstorm.

**W2 ("substrate core + grid-built halls") landed (2026-07-12, user-gated Safari after TWO gate rounds):** the spike's two-resolution substrate rebuilt as production `src/substrate/` + the `grid-built` region class — **no `@furnace/core` change**. Executed subagent-driven (15 tasks, fresh implementer + two-stage review each, 16 commits off `36f38b6` on `epic3-w2-substrate-halls`; verified independently: suite green, typecheck + check clean, byte-deterministic fixture re-bake re-run by the verifier). Landed: `substrate/` (coarse 0.5 AIR/MASONRY grids + fine 0.25 occupancy behind accessors — dense per-region, the region IS the chunk; per-face kit skin as cube-primitive `InstanceGroup`s with baked TRS mat4s + seeded FNV variant hash replacing the spike's `%5`; `prepareCarve` single-source {carved fine, set, patch, box} with the SUB+2 margin; E1 face-layer suppression; 2-piece rim collar incl. the floor-rim path; fine→voxel-proxy shell); `themes/hall.ts` (ONE parameterized stamper — sealed shell, exact-cardinal door portals, pillar lattices, dressing anchors via `rectsSurface`; mesh-trio presets); world-spec v2 (region/connector unions, `snapGridPlacement` lattice+quarter-yaw with dust assert); `connector-built.ts` (aperture; corridor with fine-grid 0.25-riser stairs + tread render; `collarBore` = W1 bore + a carve on the built grid — approach A, one authoritative grid on the built side); two-phase realize (stamp+place → connect mutations → finalize), `expandGridRegion` as the SHARED bake/load seam; grid content re-expands at load (dressing posture, zero sidecars); the committed default world became the gate world (hall-A ↔ stair corridor ↔ hall-B +1.5 m, hall-A ↔ collar-bore ↔ cave-C). `boreAxis` float-dust fix landed (backlog entry resolved+deleted). Probes: hall interior, stair corridor up/down (teeth verified by neutered `fillColumn`), collar+bore seam (THE premise — HOLDS), end-to-end gate walk; the off-axis 0.80 m spikes were isolated by a bore-spawn diagnostic to the PRE-EXISTING organic-interior KCC class, not the seam (`organic-cave-mouth-offaxis-rimride.md`, Jolt trigger). **Gate round 1 (the Safari gate caught what 1548 tests could not):** the carved opening RENDERED closed while crossable — the patch field read off-grid as solid (SN manufactured a lid at the region's grid edge) and the capsule carve's spherical end ate pillars/floor (the blob + void holes); fixed with the flat-ended CYLINDER carve (threshold Y-clip, `carveToLocal` single-sourced) + off-grid-inside-the-carve field rule + Möller–Trumbore sightline regression tests (teeth verified) — and the de-blobbed carve exposed real masked content (a pillar dead on the gate world's door axis, previously DELETED by the bug while probes ran green) → stamper door-lane validation (setup-loud), east door moved, seam fixture de-pillared. **Gate round 2:** the seam gap ring — the tunnel grid clipped exactly at the door plane, two SN rims meeting edge-to-edge — sealed by `extendA` (grid extends exactly the 0.5 shell band; interpenetration, the W1 mouth pattern on the built side; recorded in the manifest like radius/overshoot so bake/load cannot drift; capped by a regression test so tunnel rock never enters room air). Post-mortem rules (headless GEOMETRIC assertions for mesh-topology properties; new boundary condition = new premise; bug-modified content invalidates green probes): `docs/learnings/2026-07-12-w2-render-collision-divergence.md`. Honest residuals: aperture implemented but unfixtured (W3 exercises it or files coverage); colonnade-hall dressing thin (3.4/3.5 polish eye); `scatter-spacing-max-dead-config.md` filed by the verifier. Final on-branch gates: **1553 pass / 1 skip / 0 fail**, typecheck + check clean. LOCAL on `epic3-w2-substrate-halls`. NEXT: W3 (maze + World-panel assembly — the phase gate) brainstorm.

**2026-07-13 — Epic 3 · 3.3 Worlds · W3 "maze + World-panel assembly" SEALED — THE PHASE BAR MET.** The charter §1 gate passed live: a world assembled FROM SCRATCH in the editor's World panel (attach-on-add draft), baked, made the game's default via the panel, and walked in Safari game rendering — hall → stair corridor → maze → collar-bore → cave, plus the aperture box room. Dungeon half (executor session 1, `packages/dungeon`): `themes/grid-stamp.ts` extraction (shared stamp contract), `themes/maze.ts` (growing-tree + probabilistic braid, integer-only FNV/imul RNG — grep-auditably transcendental-free), the aperture derivation fix (length 0 = adjacency; the 8 m default made derived apertures structurally impossible and W2 shipped it unfixtured), `assertDisjointRegionVolumes` (charter §2.2, AABB-level, flush passes), the 5-region phase-gate `DEFAULT_WORLD` + re-baked byte-deterministic fixtures, manifest/loader maze dispatch. Probes: world-traversal grew to 7 lanes (incl. a BFS-solved maze-interior path); NEW `aperture-seam.test.ts` — whose plan-specified lanes were VACUOUS (lattice-aligned lanes thread the skin's grout gaps; even door widths centre doors ON cell boundaries) and were caught ONLY by the mandatory sabotage step, then re-derived from cell centres. Also caught in review: every door lintel skewered its own doorway (PIECE_BOX length on the face-normal axis — pre-existing W2 bug, zero tests encoded it), NaN braid full-braiding silently, maze regions self-reporting `theme:"hall"`. Editor half (session 2, `packages/editor`): `world-draft.ts` (attach-on-add tree — connectors OWN doors, portal indices can't drift; `legalKinds` filters cave-parent→grid-child, which derivation cannot place), `WorldPanel.tsx` replacing GenerationPanel (panel id `generation` kept for layout persistence), full-spec preview snapshot, per-region reroll, make-default index write (second cleanDir-free `generation.bake`; byte-parity with the committed index). Two plan-code bugs fixed by the executor: TS4111 in the test file, and addRegion throwing inside a React setState updater (render-phase, uncatchable). Gate friction was real — three live assists were needed (assemble-all-then-generate vs the "no portal 0" dead end; the Add form reading as a picker; the default offset-2 landing a door lane on a colonnade row, correctly REFUSED by W2's stamp-time validation) — six UX findings consolidated to `docs/backlog/editor-and-tooling/world-panel-w3-gate-ux-findings.md`, deferred as a set to 3.5 by user decision (no post-gate fix round). Preview usability at 5-region scale raised no concern (charter premise row closed). W2's "aperture implemented but unfixtured" residual is CLOSED (fixtured, sightline-probed, walked). Final gates: 1597 pass / 1 skip / 0 fail across 333 files, typecheck + biome + tsdoc clean, fixtures re-bake byte-clean. 26 commits over two verified executor sessions, FF-merged to master. NEXT: W4 clean-cut sweep (charter §3) — then 3.3 closes.
