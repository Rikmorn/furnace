---
summary: Epic 3 · Slice 3.3 W4 — clean-cut sweep + PHASE SEAL
sealed: 2026-07-13
seq: 19
---

# Epic 3 · Slice 3.3 W4 — clean-cut sweep + PHASE SEAL

- **Sealed:** 2026-07-13
- **Package(s):** dungeon, editor
- **Gate:** not recorded
- **Suite:** 1411 pass / 1 skip / 0 fail

**W4 ("clean-cut sweep") landed — and with it EPIC 3 · SLICE 3.3 "WORLDS" SEALS (the phase bar was met at W3; W4 was the guaranteed sweep):** the retired mesh-era machinery is deleted with zero live-path regressions. Gone (17 source/script files, 24 test files, 2 wing fixtures): the free-space placer (`layout.ts` + `chains`/`dogleg`/`locus`/`occupancy`, SA repair, `LayoutBudget`/`deadlineMs`, envelope OBBs), wing orchestration (`world.ts` `worldAttempts`/`COCKPIT_*` incl. the measured envelope table, `topology.ts`, `world-graph.ts`, `bakeWing` + wing manifest types, `wing-loader.ts`, `level.ts`), the mesh room generators (`great-hall`/`pillar-hall`/`box-room`) and the mesh connector kit (route/enclosure/threshold/landing machinery), the scripts (`measure-b2c` — ALL modes were placer-bound — `hash-world`, `bake-generated-wing`), and the editor wing session model + worker-protocol arms (`GenerationSession` block, `run`/`bake`/`attempt`; the `wing-session-model-has-no-consumers` trigger fired → delete). Suite **1596 → 1411 pass / 1 skip / 0 fail** (333 → 311 files); dungeon test files 63 → 40, editor 62 → 62 (its wing tests were rewritten to the world-only protocol, not dropped); both typechecks + `check` (0 errors; tsdoc 0 violations) clean; **`worlds/default/` and `regions/` byte-untouched — no bake ran.** Survivors relocated to honest homes (reachability-probed first — the probe CORRECTED the charter: `built.ts` is live in `cave()`'s mouth collars and stays; no "field-class retry" existed to keep, the live pipeline is deterministic end-to-end): `join`/`placeConnection`/`placePiece` → `src/placement.ts` (bodies byte-identical, new `placement.test.ts`); `Box` → `aabb.ts`; `isValidWingName` → `isValidWorldName`; `WING_NAME_RE` → `WORLD_NAME_RE`; `ThemeName`/`RegionKind` narrowed to the live vocabulary. Grid standards recorded in `dungeon-architecture.md` — and source-verification CORRECTED the charter's block twice: the door standard is 2.0×3.0 for the GRID class but the organic collar still presents 2.0×2.8 (two heights ship), and 1.0 m pillar sections do not exist (only 0.5). Reference docs rewritten deletion-driven; backlog dispositions: 7 entries closed by retirement, `dungeon/walkability-analyzer-requirements.md` FILED (the failure-class corpus + the GPU-traced constants — mounts 47.2°, stalls 49.64° — salvaged from TSDoc W4 deleted, now the only record outside git history), 2 narrowed, 3 re-targeted, 4 new findings filed from the sweep itself. **`buildGraphLegacy` in `cave.ts` was found DEAD IN PRODUCTION** (reached only by tests) **but deliberately NOT deleted** — retiring it makes `mouths` required on the exported `CaveParams` (a public-contract change) and rewrites a GPU test; filed as `docs/backlog/dungeon/cave-legacy-graph-path-is-test-only.md`, which is why its five past-due `MIGRATION (until B2 Task 9)` markers still grep. **3.4 Seeing / 3.5 Curating as chartered are SUPERSEDED-PENDING-RECHARTER** — re-evaluate against `docs/research/2026-07-13-one-field-direction.md` (world = one voxel field, algorithms as brushes, regions as semantic overlays; the flag layer is 3.4-seeing reborn, the brush editor 3.5-curating reborn) with its two pre-charter probes (analyzer corpus; chunked-field napkin) run first. **Execution lessons:** the plan's STRUCTURE held (one ordering defect, caught by the gate exactly as designed — `bake.ts`'s wing exports were providers to three files scheduled for later deletion), but its PROSE was wrong repeatedly — prescribed comments asserted a live-generation fallback the loader explicitly disclaims, colliders "re-expanding from provenance" when caves serialize them to the manifest, and a `Box` used for "connector slabs" whose producers the same tranche deleted. Two test assertions were caught VACUOUS (a 1-mesh realize fixture made `meshes.length` read `1 === 1`; deleting the `bakeWing` tests silently dropped the only non-GPU schema validation of a baked merged doc) — both restored and mutation-proved. Source-first verification, not plan-fidelity, is what caught all of it. 25 commits on `epic3-w4-clean-cut-sweep`, LOCAL and unpushed — the user merges after their own check. NEXT: run the one-field probes, then recharter.
