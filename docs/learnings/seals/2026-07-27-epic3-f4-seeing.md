---
summary: Epic 3 · One Field F4 — seeing
sealed: 2026-07-27
seq: 25
---

# Epic 3 · One Field F4 — seeing

- **Sealed:** 2026-07-27
- **Package(s):** core, dungeon, editor
- **Gate:** user-gated Safari, no fix round, variances recorded
- **Suite:** 2310 pass / 1 skip / 0 fail

**One Field F4 ("seeing") SEALED 2026-07-27, user-gated Safari (no fix round; variances recorded):** the field gained its walkability ADVISOR — never a certifier (D-F4-1: the GPU walk suites stay authoritative; nothing gates a bake on flags). **Core+dungeon (tranche A, 33 commits):** `field/analyze` (capsule-parameterized column pass over the SAME density truth the runtime colliders derive from, + voxelized placement colliders), `markUnreachable` + `detectPits`, `catalog/agent.json` as the third project→editor DATA contract (capsule + stepHeight/climbCeiling/clearance/slopeLimitDeg/skin, single-sourced through `walkability.ts`), `GeneratorDef.emits` (D-F4-15), the per-archetype collider `anchor` (D-F4-14; stalagmite base-anchored; small-props-walkable accepted as design), `PhysicsContext` + `createHeadlessPhysicsContext` (physics decoupled from the GPU context — stage-2 tests run as plain bun tests), and `walk-probe.ts`'s `analyzerVerify` (stage 2: a per-flag real-`CharacterMover` filter with trap-precedence, the levitation guard, validated spawns, budgets-as-semantics). **The slice's defining event: premise P-F4-3 was REFUTED mid-tranche and the STOP condition fired honestly** — 323 (default cave) / 1485 (runnel) candidate flags on verified-walkable ground, independently reproduced; the refutation was STRUCTURAL (on deliberately-vertical terrain, "rise > climb ceiling" and "walls nearby" describe the terrain; a trap is a CONNECTIVITY property — exactly the gap F0 named). User ruling = **pit semantics** (spec Amendment A1): ledge is ALWAYS info; `narrow` rebuilt as a true sub-cell opposing-pinch predicate (free width < 2r + skin = 0.68 m — ~64% of the old count was lattice rounding that flagged corners); the candidate class is now pit regions (directed reachability: walk/fall in, ≤ climb-ceiling out; one clustered flag per enterable-but-inescapable region) + narrow + low-clearance. **P-F4-3b then CLEARED: 0 pits in 12/12 walked cave configs, worst 3 candidates** (honest decomposition on record: 66% of the collapse = the narrow fix, a real FP kill; 34% = reclassification, not disproof). P-F4-1 held (0.052 ms/16³, ~40× headroom; cost linear in open-column height, memoized); P-F4-4 held then INVERTED (props now DOMINATE candidates as verified TRUE positives — `scatter-props-pinch-passages.md`). **Editor (tranche B, 20 commits):** the third worker (mirror `FieldStore` fed by copied chunk buffers at the four dirty sites, air-column re-analysis propagation — Amendment 2 superseded the plan's 26-neighbour halo —, latest-wins coalescing), instanced severity-tinted markers (candidate salmon-red / info amber sharing the selection color; verdict tints; `inconclusive` deliberately falls through to the band), the Flags panel (proximity clusters, filters default candidates-only, click-to-frame, per-row Verify), verify end-to-end (the worker imports `/engine.js` — TWO core instances in the realm, safe because only plain data crosses; ONE `PhysicsContext` per worker lifetime), the segment 60 m clamp, and `emits` replacing the `placesArchetypes` schema-sniffing proxy. **P-F4-2 GO on BOTH transports** (bun spike + browser: 8 verifies at ~530 ms, outcomes spread 2 trapped / 1 clear / 5 inconclusive — the spread IS the evidence the real mover ran). **Marker pixels are proven only by the committed pixel-check recipe** (`packages/editor/scripts/analyzer-pixel-check.md`: byte-identical PNG layer toggles, tints matching the predicted sRGB encode; the automated suite structurally cannot see them — bun-webgpu can't present the host's surface format; the recipe is the standing guard, invisible-line-overlays precedent). **Gate (2026-07-27):** flag-and-fix live loop, layer-toggle pixel truth, prop anchoring/blocking (bake-and-walk inside it) all passed; item 5c RULED: markers KEEP depth-testing (in-world advice, occluded like props). Variances: quiet-by-default, the verify badge, and the segment clamp accepted on machine evidence (the user was structurally blocked by the world-management hole — a GATE step blocked by an already-filed UX debt); ONE finding (selected-flag identification: chunk-sized frame box, direct-click-to-select wanted) → `field-f4-gate-ux-findings.md`, polish stage. **Process record:** FOUR plan defects found by building (a doc'd-bitset-vs-packed encoding lie, the halo under-bound, a docs-ordering trap, and a spawn detector the plan specified that provably cannot reject any pose); the dominant defect class across both tranches was COMMENT ACCURACY, not code (~20 false comments, several load-bearing → rules in `docs/learnings/2026-07-27-comment-accuracy-defect-class.md`). Suite 2076 → **2310 pass / 1 skip / 0 fail** (367 files); merged FF `1ad745c9 → 3012d96b` (tranche A `30a7f1df → 1ad745c9`) + stage-2 docs `825f1926` + this seal. NEXT: F5 "scale". The typescript.md third-bypass-class question and the fired editor-extensions-seam contract trigger are filed, undecided.
