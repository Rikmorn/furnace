# Epic 3 · One Field F2b — "the palette"

- **Sealed:** 2026-07-21
- **Package(s):** cookbook, core, editor (dungeon stated byte-untouched this slice)
- **Gate:** user-gated Safari, three rounds, third accepted
- **Suite:** 1693 pass / 1 skip / 0 fail

**F2b SEALED (user-gated Safari over THREE rounds, third accepted 2026-07-21; merged to master FF `8f3f2481 → 3f39a1dd` + this seal commit):** the tool system over the material field — the second half of the F2 spec (§3 + the §3.7 plan-time QoL register dispositioned with the user post-F2a). Core: the `FieldOp` union + op-LIST undo entries (one ⌘Z per generator commit), brush masks (organic/kit/class/solid-only/selection-embedding — the replace idiom acceptance-tested), the smooth effect (max-delta-clamp strength = the thin-wall guard; density-only), hollow fill (non-destructive shell band), deterministic budgeted selection (region/flood-material/flood-void, loud truncation), the staged-generator registry (hall + maze ported donor-bit-parity, integer-only RNG) + `commitGenerator` with full provenance (`GeneratorEntity`; reconfigure = F3), raycast `maxY`. Editor: brush chassis + shortcuts (eyedropper/momentary), selection tools with live box preview + amber overlay + Reselect, layers + remesh-clip slice view (capped cuts for free; all targeting slice-coherent), worker v3 stamp-preview (scratch-store hologram ghosts, Enter/Esc, lattice nudge), palette/swatches/inspectors/entities panel restructure with a bounded controls region, field-first default layout, catalog-gated Load; host math extracted to tested modules (F2a carry-overs closed). Cookbook `field` demo proves the public API stands alone; `@furnace/core/field` graduated in `core-modules.md`. Suite 1523→**1693 pass / 1 skip / 0 fail**; budgets: evaluate ≤0.5 ms (ceiling 50), flood ≤75 ms (ceiling 100), stamp preview ~0.9 ms e2e. **The gate's defining find: TWO core rendering bugs, not editor bugs** — `frame.drawLines` built an invalid pass on MSAA contexts (every field-viewport line overlay had been silently dropped SINCE F1 — grid, dig ring, selection; the class was even pre-filed in backlog and annotated in bowling, unnoticed) and blended draws recorded before instanced (ghosts behind kit tiles). Both fixed with GPU regression tests (error-scope + blend-order); rules recorded in `docs/learnings/2026-07-21-invisible-line-overlays.md` (pixel/error checks for visual features, grep backlog first, name expected pixels in gate scripts, headless-playwright repro is decisive). Three executor fix rounds ran subagent-driven in a separate cheap-model process with independent verification here each time. Gate variances recorded honestly: bake-and-walk WAIVED by the user this slice (the bake path is F2a-sealed and untouched by F2b; dungeon byte-untouched), stamp-anchor confusion + selection ergonomics + nudge focus trap + editor AA toggle deferred AS A SET → `docs/backlog/editor-and-tooling/field-f2b-gate-ux-findings.md` (F3 placement items; F4 recommended recharter as "seeing & the cockpit pass"). NEXT: F3 "smart objects & the cave".
