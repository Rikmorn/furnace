# Epic 3 · One Field (the 3.4+ recharter) — charter + F0 + F1 "the medium"

- **Sealed:** 2026-07-15
- **Package(s):** core, dungeon, editor
- **Gate:** user-gated Safari
- **Suite:** 1476 pass / 1 skip / 0 fail

**The recharter landed and its first two rungs SEALED (user-gated Safari, merged to master FF `3636fb3 → f0c120d` + 3 seal-side commits):** 3.4+/3.5-as-chartered are replaced by the **One Field phase** (charter 2026-07-14, sub-slices F0–F6): the world is ONE sparse chunked voxel field in `@furnace/core/field`, everything else is entities (the project supplies the catalog; the editor is world-native — two contracts, catalog project→editor and artifact editor→game), tools split into immediate brushes vs staged region-scoped generators that commit as reconfigurable smart-object entities, ops are the only mutation path (coordinate-keyed, bounded influence, per-region snapshots later; the RUNTIME never replays), mega-world invariants from day one, never-blocky as a stated bar, and a **de-bias rule**: no design decision cites "we already have X" — existing code is donor material. **The charter was research-gated:** a five-lane precedent sweep (committed: `docs/research/2026-07-14-field-precedent-research.md`) hunted disconfirming evidence BEFORE commitment and changed the design — the pure static walkability analyzer was REFUTED by the record (Recast's column model passes the wedge class by design; EA SEED and The Witness both abandoned static-only) → the hybrid (column flags + REAL-controller swept probes); Valheim's verified op-replay collapse + Dreams' edit-list scale proof shaped the persistence conditions; the measured mesher baseline (M1: old mesher 21–32 ms per 32³, 3–5 ms per 16³) picked the 16³ mesh unit. **F0 (analyzer corpus probe):** zero false-negative misses on the validity-proven trap corpus (fixtures must FIRST trap the real mover — Walk-Monster fidelity; a vacuous absolute-coordinate assert was caught and rebuilt as a control-vs-hazard differential), BUT the premise disconfirmed by measurement: 2 of 4 specified classes don't reproduce (the mover's real climb ceiling is ~0.7 m, not `STEP_HEIGHT` 0.4), stage 2 re-justified as a false-positive filter, and the known-good `DEFAULT_WORLD` load is unaffordable as-is (540 stage-1 flags; the zero-traps gate was REFRAMED to characterization, not passed) — **the analyzer cannot self-certify F4** (needs capsule-aware navigability; fallback ladder intact). **F1 ("the medium", the vertical thin slice):** core `field/` module (16³ Int8 chunks, solid-by-default + uniform elision; dig ops + chunk-keyed undo; chunked Surface Nets with 18³ aprons + owned-crossing quads — watertight seams TESTED against analytic crossing counts; voxel DDA; per-chunk shell colliders; pure `bakeFieldWorld`); editor Field panel + `FieldHost` (fly camera, dig strokes, flat-shaded `normalColor` vs headlamp toggle, ground reference; host threaded through the `/engine.js` runtime channel — the plan's direct-import draft would have broken the project-first invariant, and the no-engine-leakage guardrail is now machine-enforced against core/field-protocol/viewport-host value-imports); a third bundle entry remesh worker (engine-direct, transferables, dirty-SET coalescing; **1.13 ms median per 16³ chunk on the M1** — the plan's <1 ms prediction was FALSE at first measure, 5.7 ms, fixed by an output-identical hot-loop pass); ONE new daemon command (`field.load`) — saves/bakes ride the existing destination-agnostic `generation.bake`; dungeon `field-world.ts` loads manifest v2 (baked `.fmesh` meshes + colliders derived AT LOAD from shipped chunk density — never serialized), branching before `assertCompatible` with the v1 path byte-identical; headless proof = dig→bake→load→walk GPU test. **Pre-gate mover fix (the adjacent-finding rule fired):** F0's surfaced SHIPPED levitation bug (`applyGravity` lifted STEP_HEIGHT blind; a penetrating start pose's toi-0 read as "support at lift height" → +0.4 m/frame while grounded on sub-2.2 m-clearance floors — exactly F1's dug tunnels) was fixed by capping the lift at the UPWARD-PROBED free headroom — and the repro exposed a SECOND latent defect, exact-contact rest stalling the horizontal slide dead on flat voxel floors (toi-0 with arbitrary normals at voxel-cell boundaries), fixed with a `REST_GAP` 2 cm contact offset (standard KCC posture). Both guarded by `tests/char-move-levitation.gpu.test.ts`; probe guards retained as regression insurance; the F4 note records that the pre-fix false-positive numbers OVERSTATE the load. **The gate:** editor works, save/bake works, dug world walked from inside (the F1 gate sentence verbatim); findings → dig-tool feel backlogged with an F2 trigger (`field-dig-tool-feel.md` — bite depth, embedded-eye fixed-depth carve, target preview: v0 design choices, not bugs) and the recurring zero-size-canvas init latch FIXED (`lib/init-when-sized.ts` one-shot deferral on all three hosts). Numbers: suite 1411 → **1476 pass / 1 skip / 0 fail** (325 files), +5,983/−32 over 28 branch commits (25 executor + 3 session), both typechecks + biome + `check:tsdoc` (21 modules) clean, `worlds/` fixtures byte-untouched throughout; merged master re-verified independently. `core-modules.md` carries a v0-unstable stub for `field` (full docs pass + cookbook = F2, a charter decision). NEXT: F2 "tools & materials" brainstorm (material classes + palette encoding, kit-skin dispatch — charter P4 —, brush chassis, selection, stamp brushes as the first generator entities).
