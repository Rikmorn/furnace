# Epic 3 · One Field F2a — "the material field"

- **Sealed:** 2026-07-16
- **Package(s):** core, dungeon, editor
- **Gate:** user-gated, Safari
- **Suite:** 1523 pass / 1 skip / 0 fail

**F2a SEALED (user-gated Safari 2026-07-16, merged to master FF `ae50b5b → 1ed46e2` + seal commit):** the field learned materials — the first half of the F2 "tools & materials" spec (ONE spec, two sub-slices; F2b "the palette" executes next against this as-built). **The design was research-gated twice over:** the slice brainstorm locked per-quad class dispatch over built-structure-as-overlay (violates L1) and a parallel W2 substrate (two solidity truths — the L12 trap), plus the phase jurisdiction rule **"if you can dig it, it's field"** (placed entities carry their own colliders and compose in the same physics world — no bake-time collision resolution exists); then a four-lane tool-UX precedent sweep (committed: `docs/research/2026-07-15-f2-tool-ux-precedent-research.md`) validated the F2b-facing choices BEFORE the spec locked them — nothing refuted; absorbed: effect-as-tool-identity (chassis stays the internal model), Photoshop Reselect over full selection-undo, hologram-blue/red ghost conventions, smooth as a max-delta-clamped density blur; flagged as OUR unprecedented risks: brush-level lattice snap, flood-select void. **Plan-time discoveries (the source-first pass paying rent):** the spec's withheld-kit-quads + damage-bucket mechanism was REPLACED pre-plan by the **backing bucket** (kit crossings keep their SN quads under a mortar tint — proud panels have reveals that would otherwise open onto void, the W2 `backing-masonry-fallback` concern solved structurally, and the backing IS the damage surface); point-sampled SDF fields don't decompose into W2's cell-grid faces → the **derived coarse view** (0.5 m cells classified by their CENTER sample: kit-solid / suppressed-kit / organic / air; W2's panel/post/collar rules run on it nearly verbatim; a 20³ apron pair exactly covers the neighbor reads, keeping the worker stateless); the planner's own fixture geometry twice violated its own lattice rule — caught at self-review, not by the executor. **Execution (13 commits, +4,476/−368):** material channel (uniform|indexed palette encoding behind accessors), `DigOp`→`BrushOp` (dig/fill/paint, two-channel chunk-keyed undo, kit lattice validation setup-loud + re-checked on replay), `meshChunkField` per-class buckets, `skinChunkKit`, FFM1 material files + embedded-table artifact, catalog (`catalog/materials.json` + setup-loud frontend parser, type-only imports — leakage guard held), worker protocol v2, host tools + dig-feel fixes (bite 0.7·r, carve-from-eye, ghost ring, radius 1.25), dungeon loader + instanced kit. **Premise P4 PROVEN headless, stop condition never fired:** topology fixtures A–D — the reviewer added fixture D (hole through a seam-straddling wall) after catching that the collar pass's apron reach exceeds the panel pass's, so C's panel proof didn't cover cross-chunk collar; all counts analytic, no hacks. Budget **0.71 ms median per 16³ with dispatch + skinning** (5 ms ceiling, ~7× headroom). Walk probe: through the gap AND blocked by the wall with **zero collision-code change** (masonry blocks as solid density). Suite 1476 → **1523 pass / 1 skip / 0 fail** (329 files); both package typechecks + biome + tsdoc clean; independently re-verified on the merged tree. **Gate findings:** materials visible, world built; ONE finding — large kit fills read as "deleting the cave" (solid volumetric fill semantics vs a structure-placing mental model) → designed-v0, backlogged as `fill-tool-solid-volume-surprise.md`, the seed of F2b's QoL register (user: "a lot of quality of life i would like to do around this"). **Dispositions:** `field-dig-tool-feel.md` RESOLVED + deleted; new entries: kit-render duplication (2nd occurrence, host+loader — F2b's ghost previews are the likely third consumer), F2a→F2b carry-over (field-host at ~1 kloc → extract at F2b; catalog-wins vs embedded-table decided: catalog wins, Load-gating hardening to F2b), variantHash low-bit mixing (F5 trigger). `worlds/index.json` was gate-retargeted and restored again (second arc running — the Bake-default gate flow touches it by design; restoring it is a standing seal chore). NEXT: F2b "the palette" — plan written fresh against this as-built (chassis + palette UI + selection + stamp generator entities + layer vis/slice view), inputs: the F2 spec §3, the fill-tool + carry-over backlog entries, the research refinements.
