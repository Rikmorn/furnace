---
summary: Epic 2 · Slice 2.2.3b — "Colliding fixtures + interactive props"
sealed: 2026-07-06
seq: 8
---

# Epic 2 · Slice 2.2.3b — "Colliding fixtures + interactive props"

- **Sealed:** 2026-07-06 (date not recorded in the original; this is the extraction date)
- **Package(s):** dungeon (prose states "No `@furnace/core` change")
- **Gate:** collision feel stays a visual gate (browser not named)
- **Suite:** not recorded

**Epic 2 · Slice 2.2.3b ("Colliding fixtures + interactive props") then landed:** scatter layers gained a `collision` posture (`"solid" | "dynamic"`, absent = decorative ghost) on `ScatterLayerSpec`/`InstanceGroup` (+ `InstanceGroup.placements` = the baked-frame pos/rot/scale). `realize.ts` interprets it: `solid` → one static primitive collider per instance (`colliderFor`: cube→cuboid / sphere→ball / cylinder→cylinder, uniform-scaled, no trimesh → ghost-free); `dynamic` → one shovable rigid body per instance + per-frame instanced-render sync (`update()`) + an addressable `DynamicProp` record (the seam for later pickup/throw/`query` verbs). Posture consumes no RNG (toggling it never reshuffles placements); dynamic props spawn lifted (`DROP_MARGIN`) and settle via friction/damping/auto-sleep. `compose.ts placeRoom` transforms room groups' `placements` into world frame. Content: cave `spires` → solid, box-room `roomCrates` → dynamic. The hand-placed `props.ts` was **retired** — loose crates are now a dynamic scatter layer (single source of truth). No `@furnace/core` change. Headless: `tests/scatter.test.ts` (posture/placements carry + determinism + dynamic lift), `tests/scatter-realize.gpu.test.ts` (solid supports a dropped ball; dynamic shove + `update()` tracks), `tests/compose.test.ts` (`placeRoom` world-frame placements incl. a rotation cross-check), `tests/cave.test.ts` (spires solid); collision feel stays a visual gate.
