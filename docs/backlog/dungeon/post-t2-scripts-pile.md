# The dungeon `scripts/` pile — post-T2 remainder

Named at the foundations programme's disposition sweep (2026-08-04); never filed; filed
at the T3 objectives audit (2026-08-08). Foundations T2 retired dungeon v1 and the scene
system; `packages/dungeon/scripts/` kept accumulating one-off bake/measure/probe scripts
whose referents partly died with it. Inventory is spec-era — re-derive which scripts
still run and which reference deleted machinery at pickup.

## Trigger to revisit

- T5's backlog/hygiene prune (the natural home for a scripts sweep).
- Any dungeon work that reaches for a script and finds it broken.

## Reference

- `packages/dungeon/scripts/`; `packages/dungeon/README.md` (current-state).
