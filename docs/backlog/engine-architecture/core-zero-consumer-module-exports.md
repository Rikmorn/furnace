# Core's ~110 zero-consumer module-index exports — a pruning question entangled with T4

Named at the foundations programme's disposition sweep (2026-08-04); never filed; filed
at the T3 objectives audit (2026-08-08). **This one is a T4 planning input.** The
programme's core sweep counted ~110 names exported from module `index.ts` files with
zero consumers anywhere in the workspace. The deletion-pass instinct says prune — but
the MCP surface (T4) may PROJECT some of this vocabulary to agents, which would give
zero-consumer exports their first consumer. Pruning before T4 decides what projects
would delete surface T4 then re-adds. Count is spec-era (sweep scope: the T1 core
package audit) — re-derive at pickup.

## Trigger to revisit

- **T4 planning** (imminent): the projection decision names which vocabulary MCP
  exposes; everything not named becomes a clean deletion candidate for T5's prune.

## Reference

- `packages/core/src/*/index.ts`; `docs/reference/core-modules.md`;
  `docs/backlog/editor-and-tooling/editor-ai-integration-milestone.md` (the T4 donor).
